<h1>Listing products</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Price</th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.title %></td>
    <td><%= product.price %></td>
  </tr>
<% end %>
</table>
